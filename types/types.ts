export interface Product {
  _id: string;
  name: string;
  price: number;
  description: string;
  image: string;
}

export interface HeroData {
  title: string;
  subtitle: string;
  videoUrl: string;
}